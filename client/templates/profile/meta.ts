import type { ScreenTemplateEntry } from "../types";

export const meta: ScreenTemplateEntry = {
  id: "profile",
  route: "/(main)/(demos)/screen-profile",
  label: "Profile",
  description: "Avatar, stats, sections",
  icon: "user",
  order: 20,
};
