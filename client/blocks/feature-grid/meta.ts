import type { BlockEntry } from "../types";

export const meta: BlockEntry = {
  id: "feature-grid",
  label: "Feature grid",
  description: "Icon + title + copy, 1–3 columns by width",
  category: "marketing",
  recipe: ["Icon", "StyledText"],
  icon: "layout-grid",
  order: 20,
};
