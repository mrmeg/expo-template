// The `(tabs)` layout for web, and the non-platform fallback Expo Router
// requires next to a platform file: a plain stack inside the drawer shell.
// iOS and Android pick `_layout.native.tsx`, the native tab bar.
export { default } from "@/client/features/navigation/TabLayoutWeb";
