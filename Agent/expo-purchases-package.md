---
status: ready
mode: AFK
base-branch: dev
blocked-by: -
pr: -
---

# `@mrmeg/expo-purchases`: shared RevenueCat paywall package (`packages/purchases`)

## Goal

A third workspace package, `packages/purchases` → `@mrmeg/expo-purchases` 0.1.0, that lifts the RevenueCat client, entitlement-state, gating, and pure webhook/ledger code that NeuroSpicy and Mindmap each carry today into one publishable package mirroring `packages/media` (structure, build, tests, docs, exports, release tooling). No consumer migrates in this ticket; nothing publishes.

## Context

Design source: `/Users/mattmegenhardt/Development/portfolio/monetization/2026-09-22-brief.md` §A. Lift code, do not rewrite. Source repos are READ-ONLY:

- NeuroSpicy `/Users/mattmegenhardt/Development/neurospicyos/neurospicyos`: `client/services/subscription/revenuecat.native.ts` (configure/logIn/logOut/restore/`presentPaywall` via `RevenueCatUI`, `PaywallOutcome` enum), `revenuecat.web.ts` (stub), `entitlementSnapshot.ts` (user-scoped persisted snapshot, version-gated), `server/revenueCatEventProcessor.ts` (`RcWebhookEvent` fields, `buildRcLedgerRows`, `isRefundCancellation`, `mapRcStatus`, `providerSubscriptionId`), `server/subscriptionLedger.ts` (`SUBSCRIPTION_EVENT_TYPES`), `app/api/subscription/revenuecat-webhook+api.ts` (401/400/200 route semantics), `docs/subscription-billing.md` "RevenueCat" table, `docs/app-store-resubmission.md` (3.1.2(c): Terms + Privacy links on the paywall), `docs/revenuecat-setup-runbook.md` (fixed values, env vars).
- Mindmap `/Users/mattmegenhardt/Development/mindmap` at `origin/dev` (b5b2801): `client/features/pro/purchasesModule.native.ts` / `purchasesModule.ts` (key-gated dynamic import, web twin returns null), `purchasesClient.ts` (`toCustomerState`, `configurePurchases`, `identifyPurchases`, `resetPurchasesIdentity`, `subscribeToCustomerState`, restore outcomes), `proStore.ts` (`selectIsPro`, `selectProUntil`, dev override), `PurchasesProvider.tsx` (effects: configure when signed in, logIn/logOut on user change, listener, server entitlement mirror), `usePro.ts` (`requirePro(feature)` → push `/paywall`), `convex/revenuecatEvents.ts` (`isAuthorizedWebhook`, `timingSafeEqual`, `parseRevenueCatWebhook`, `reduceEntitlement`, `revokedByTransfer`), tests `client/features/pro/__tests__/{proStore,purchasesClient}.test.ts`, `test/convex/revenuecatEvents.test.ts`, `docs/pro-subscription.md` (identifier table, 7 human setup steps).

Verified in this repo (`origin/dev` 70f279a):

- `packages/media` is the template: `package.json` (`type: module`, `exports` map with `types`+`default`, `files`, `publishConfig.access: public`, every peer optional via `peerDependenciesMeta`, scripts `typecheck`/`test`/`build`/`publish:dry-run`), `tsconfig.json` (extends root, `noEmit`), `tsconfig.build.json` (`outDir dist`, `rootDir src`, `module ESNext`, `moduleResolution Bundler`, excludes `__tests__`), build = `rm -rf dist && tsc -p tsconfig.build.json && node ../../scripts/fix-package-esm.mjs media`, tests via root `jest.config.js` (`jest-expo`, `Platform.OS === "ios"`, `.native.ts` resolves over `.ts`), docs `README.md`, `CHANGELOG.md`, `LLM_USAGE.md`, `llms.txt`, `llms-full.md`, `LICENSE`.
- Package tables that must gain a `purchases` entry: `scripts/run-package-script.mjs` `PACKAGES`; root `package.json` aliases `purchases:{typecheck,test,build,pack,consumer-smoke,release}` (`scripts/__tests__/runPackageScript.test.ts` pins `PACKAGE_SLUGS`, the `EXPECTED` alias map, and the publish-workflow list); `scripts/release-package.mjs` `PACKAGES`; `scripts/fix-package-esm.mjs` `PACKAGES` (`platformSuffixes: ["native"]`); `scripts/check-package-consumer.mjs` `PACKAGES` (export checks, `requiredDocs`, fixtures); `scripts/check-package-peer-compatibility.mjs` (`checkPackage("purchases")`, all peers optional like media); `scripts/package-compatibility-profiles.mjs` (`purchases` profiles); `scripts/check-package-consumer-profile.mjs` (its `App.tsx` branches `ui` / else-media — add a `purchases` branch); `.github/workflows/package-compatibility.yml` (paths + matrix); `scripts/__tests__/packageToolingScripts.test.ts` asserts the sorted lists `"lint, media, ui"` and `"media, ui"` — become `"lint, media, purchases, ui"` and `"media, purchases, ui"`.
- Resolution wiring: root `tsconfig.json` `paths` and `jest.config.js` `moduleNameMapper` + `collectCoverageFrom` list media entrypoints explicitly; add the purchases ones.
- Docs wiring: `scripts/build-llms-full.mjs` `sources` (add `packages/purchases/LLM_USAGE.md`), root `llms.txt` (Docs list), `AGENTS.md` (Docs table row, Tech Stack row), `README.md` (`/packages/media` architecture line, "GitHub Publishing" paragraph near line 321, coverage sentence line 159), `CONTRIBUTING.md:42` coverage sentence, `docs/template-modernization-guide.md:17,39` package list. `bun run docs:llms` regenerates `llms-full.txt`/`llms-examples.txt`; `docs:llms:check` fails otherwise.
- SDK facts (npm, 2026-09-23): `react-native-purchases` 10.10.1 and `react-native-purchases-ui` 10.10.1 (UI pins the exact same purchases version). Types come from `@revenuecat/purchases-typescript-internal` 19.2.0: `PAYWALL_RESULT` = `NOT_PRESENTED | ERROR | CANCELLED | PURCHASED | RESTORED`; `RevenueCatUI.presentPaywall({ offering?, displayCloseButton?, fontFamily? })` takes a `PurchasesOffering` object, `presentPaywallIfNeeded({ requiredEntitlementIdentifier, offering? })`; `Purchases.configure({ apiKey, appUserID? })`, `logIn(id) → { customerInfo, created }`, `logOut() → CustomerInfo`, `isAnonymous()`, `getCustomerInfo()`, `getOfferings() → { all, current }`, `restorePurchases() → CustomerInfo`, `add/removeCustomerInfoUpdateListener`, `setAttributes`, `setLogLevel(LOG_LEVEL)`; `CustomerInfo.entitlements.active[id]: PurchasesEntitlementInfo { isActive, willRenew, expirationDateMillis, productIdentifier, ... }`, `managementURL`, `originalAppUserId`. Consumers: Mindmap `react-native-purchases ^10.10.0`, NeuroSpicy `^10.9.1` both SDKs.
- Publishing: `publish-media.yml` fires on push to `main` (path `packages/media/package.json`) and on dispatch; `publish-lint.yml` is dispatch-only with a comment explaining a first publish cannot use trusted publishing. `main` publishes on push: never touch it. No `npm publish` in this ticket.
- Root deps already present: `zustand ^5.0.15`, `@types/react ~19.2.18`, `typescript ~6.0.3`, `@types/node`. `react-native-purchases*` are not installed anywhere in this repo.

Constraints: no consumer migration; no `Agent/` files in the PR diff; no publish and no workflow that publishes on merge to `dev` (the new publish workflow is `workflow_dispatch`-only, like `publish-lint.yml`); `react-native-purchases` / `react-native-purchases-ui` are optional peers `>=10.0.0 <11.0.0`; every peer optional (server consumers install none); `/server` must import nothing from `react`, `react-native`, or the SDKs; never print secret values; the RC public keys are passed in by the app (`iosKey`/`androidKey`), the package reads no `process.env`.

## Work

1. Scaffold `packages/purchases` from `packages/media`: `package.json` (`@mrmeg/expo-purchases` 0.1.0, description "Reusable RevenueCat purchases client, entitlement state, paywall gating, and pure webhook/ledger helpers for Expo apps", keywords `expo react-native revenuecat in-app-purchases paywall subscriptions webhook`, `repository.directory packages/purchases`, exports `.` and `./server`, `files` as media, scripts `typecheck`/`test` (`jest --config ../../jest.config.js packages/purchases/src --runInBand --watchman=false`)/`build` (`... fix-package-esm.mjs purchases`)/`publish:dry-run`; peers all optional: `react >=19.2.0 <20.0.0`, `react-native` (copy media's range), `zustand >=5.0.0 <6.0.0`, `react-native-purchases >=10.0.0 <11.0.0`, `react-native-purchases-ui >=10.0.0 <11.0.0`; devDependencies `@types/react`, `typescript`, `react-native-purchases 10.10.1`, `react-native-purchases-ui 10.10.1`), `tsconfig.json`, `tsconfig.build.json` (copy media; `jsx: react-jsx`), `LICENSE` (copy), `doctor.config.json` optional. Run `bun install` (lockfile updates; commit `bun.lock`). Then confirm the template app's native autolinking does not pick the SDKs up: `bunx expo-modules-autolinking react-native-config --platform ios --json` and `--platform android`, plus `bunx expo-modules-autolinking search --platform ios --json`, run from the repo root must not mention `react-native-purchases`; if either does, drop the two SDK devDependencies, add `packages/purchases/types/react-native-purchases{,-ui}.d.ts` declaring only the members used, include them via `tsconfig.json` `include`, and note it in the PR body.
2. Client core, `src/`:
   - `types.ts`: `PurchasesConfig { entitlement: string; offering?: string; iosKey?: string; androidKey?: string; logLevel?: "verbose"|"debug"|"info"|"warn"|"error"; onError?: (error: unknown, context: string) => void }`, `CustomerState { isActive: boolean; until: number | null; willRenew: boolean; productId: string | null; managementUrl: string | null; appUserId: string | null; activeEntitlements: string[] }`, `PaywallOutcome = "purchased" | "restored" | "cancelled" | "error" | "not_configured" | "not_presented"`, `RestoreOutcome = { kind: "restored" | "none"; customer: CustomerState } | { kind: "failed"; message: string } | { kind: "not_configured" }`, `PurchasesClient` interface.
   - `sdk.native.ts` / `sdk.ts` (lift Mindmap `purchasesModule.*`): `loadPurchasesSdk(): Promise<PurchasesSdk | null>` and `loadPaywallUi(): Promise<PaywallUi | null>` with cached `import("react-native-purchases")` / `import("react-native-purchases-ui")`; the web `.ts` twin resolves `null` and has no imports; `resetSdkCache()` test seam. The native loader is only called after a key check, so an unconfigured build never touches the native module.
   - `customerState.ts`: `toCustomerState(info: CustomerInfo, entitlement: string): CustomerState` (lift Mindmap `toCustomerState`; add `activeEntitlements = Object.keys(info.entitlements.active)`), `resolvePlatformKey(config, platform)`.
   - `createPurchases.ts`: `createPurchases(config): PurchasesClient` with `isConfigured()` (a key exists for `Platform.OS`), `configure(appUserId?)` (once; `configure({ apiKey, appUserID })`; `setLogLevel` when `logLevel` given; returns `boolean`), `logIn(appUserId) → CustomerState | null` (skips when already that id), `logOut()` (only when `!isAnonymous()`), `getCustomerState()`, `restore() → RestoreOutcome`, `presentPaywall({ offering? })` (resolve the id through `getOfferings().all[id] ?? current`, call `RevenueCatUI.presentPaywall({ offering })`, map `PAYWALL_RESULT` → `PaywallOutcome`; lift NeuroSpicy), `presentPaywallIfNeeded({ offering? })` (`requiredEntitlementIdentifier: config.entitlement`; `NOT_PRESENTED → "not_presented"`), `subscribe(listener) → unsubscribe` (lift Mindmap), `setAttributes(record)` (lift NeuroSpicy). Every method resolves `not_configured`/`null`/`false` instead of throwing; SDK errors go to `onError` and `console.warn` in `__DEV__`.
3. Entitlement store, `src/entitlementStore.ts` (zustand `createStore`, lift Mindmap `proStore` + NeuroSpicy `entitlementSnapshot`): state `{ sdkStatus: "idle"|"ready"|"unavailable"; customer: CustomerState | null; serverUntil: number | null; snapshot: EntitlementSnapshot | null; hydrated: boolean; devOverride: boolean }`; actions `setSdkStatus`, `applyCustomerState(customer)`, `applyServerEntitlement(until)`, `hydrate(userId)`, `clear(userId)` (also removes the persisted snapshot), `setDevOverride` (no-op unless `__DEV__`), `reset`. `EntitlementSnapshot { version: 1; userId: string | null; savedAt: number; isActive: boolean; until: number | null }` persisted through an injected AsyncStorage-shaped `EntitlementStorage { getItem; setItem; removeItem }` under `${prefix}${userId ?? "anonymous"}`; written on every `applyCustomerState`/`applyServerEntitlement` (revocations included), rejected on version or user mismatch, `hydrated` becomes `true` after the read (immediately without storage). Pure `resolveEntitlement(state, now, entitlement?) → { isEntitled, until, source: "dev"|"device"|"server"|"snapshot"|"none" }`: dev override (`__DEV__` only) → device `customer.isActive` (or `activeEntitlements.includes(entitlement)` for a non-default id) → `serverUntil > now` → snapshot only while `customer === null && serverUntil === null` and `snapshot.isActive && (until === null || until > now)` → none; `until` is the max known expiry. `createEntitlementStore({ storage?, storageKeyPrefix? })`.
4. React layer, `src/react/`: `PurchasesProvider({ client, store, userId, serverUntil?, onBlocked?, children })` (lift Mindmap `PurchasesProvider` effects: configure when `userId` is non-null, `setSdkStatus`, subscribe to customer updates, initial `getCustomerState`, `logIn` on user change / `logOut` + `clear` when it becomes null, `hydrate(userId)`, mirror `serverUntil` prop through `applyServerEntitlement`); `useEntitlement(entitlement?)` → `{ isEntitled, until, source, hydrated, isConfigured, isReady, customer, restore, presentPaywall, presentPaywallIfNeeded }`; `useRequireEntitlement(feature: string) → () => boolean` (true when entitled, otherwise calls the provider's `onBlocked(feature)` and returns false; lift Mindmap `requirePro`); `<PaywallGate feature? entitlement? fallback? onBlocked?>` renders children when entitled, else `fallback ?? null` and fires `onBlocked`/provider `onBlocked(feature)` once per block. The package does not import `expo-router`; the app's `onBlocked` pushes its paywall route.
5. Server helpers, `src/server/` (pure, no RN/React imports): `isAuthorizedWebhook`, `timingSafeEqual` (lift Mindmap); `parseRevenueCatWebhook(body) → RevenueCatWebhookEvent | null` returning the camelCase union of the fields both apps read (`id, type, appUserId, originalAppUserId, aliases, productId, entitlementIds` (fold legacy `entitlement_id`), `periodType, purchasedAtMs, expirationAtMs, eventTimestampMs, environment, store, originalTransactionId, cancelReason, expirationReason, price, priceInPurchasedCurrency, currency, renewalNumber, isTrialConversion, countryCode, offerCode, transferredFrom, transferredTo, raw` (event minus `subscriber_attributes`)); `reduceEntitlement(current, event, { entitlement })` + `revokedByTransfer` (lift Mindmap, parameterised); `LEDGER_EVENT_TYPES` and `buildLedgerRows(event, { userId, previouslyExpired? }) → LedgerRow[]` (lift NeuroSpicy `buildRcLedgerRows`: `trial_started`/`purchased` (+`reactivated`), `trial_converted`/`renewed`, `cancel_scheduled` (+`refunded` on `isRefundCancellation`), `uncanceled`, `expired`, `billing_issue`, `product_changed`, `refund_reversed`; camelCase row `{ userId, provider: "revenuecat", eventType, providerEventId, providerEventType, providerSubscriptionId, productId, store, periodType, amountCents, currency: "usd", renewalNumber, cancelReason, environment, occurredAt (ms), payload }`); `isRefundCancellation`, `deriveSubscriptionStatus(event) → "trialing"|"active"|"past_due"|"canceled"` (lift `mapRcStatus`), `providerSubscriptionId`; `createWebhookHandler({ secret, onEvent, entitlement? }) → (request: Request) => Promise<Response>` with NeuroSpicy's route semantics (500 when no secret, 401 bad header, 400 bad body/missing `event.id`/`type`, 200 after `onEvent(event)`, 500 when `onEvent` throws).
6. Entry files: `src/index.ts` (client + store + react + types), `src/server/index.ts`. `fix-package-esm.mjs` gets `purchases: { dist: "packages/purchases/dist", platformSuffixes: ["native"] }` so `./sdk` stays extension-less.
7. Tests first (`src/**/__tests__`, jest-expo, mock the SDKs with `jest.mock("react-native-purchases", () => ({ __esModule: true, default: mockSdk, LOG_LEVEL: {...} }))` and `jest.mock("react-native-purchases-ui", ...)` with `PAYWALL_RESULT`): `server/webhook.test.ts` (lift Mindmap `revenuecatEvents.test.ts`: auth, timing-safe compare, parse incl. legacy `entitlement_id` and TRANSFER, reduce grants/revoke/stale/not-entitled/keeps product id, transfer revoke), `server/ledger.test.ts` (lift NeuroSpicy: one row per RC type from the table in `docs/subscription-billing.md`, `trial_started` pins amount 0, cents rounding 8.295 → 830, null price → null, composite `providerSubscriptionId` fallback, refund heuristic requires `CUSTOMER_SUPPORT` and `expirationAtMs <= eventTimestampMs`, `reactivated` only with `previouslyExpired`, `subscriber_attributes` stripped from `payload`, unknown type → `[]`), `server/handler.test.ts` (500/401/400/200/500 paths), `sdk.web.test.ts` (load `../sdk.ts` by explicit filename through `require` so Jest skips platform resolution → both loaders resolve `null`; `createPurchases` with no key on web returns `isConfigured() === false`, `presentPaywall() === "not_configured"`, `restore().kind === "not_configured"`), `createPurchases.test.ts` (key gate per platform, configure once, logIn skips same id, logOut only when not anonymous, paywall result mapping incl. `NOT_PRESENTED`, offering resolution by id, listener unsubscribe), `customerState.test.ts`, `entitlementStore.test.ts` (resolve precedence device > server > snapshot, snapshot ignored once a live source reports, expired snapshot, user/version mismatch rejected, persistence on apply and removal on `clear`, `hydrated`, dev override only in `__DEV__`), `react/gate.test.tsx` (RNTL: `PaywallGate` children vs fallback, `onBlocked` fires once, `useRequireEntitlement` true/false + `onBlocked(feature)`, `PurchasesProvider` configures only with a user id and mirrors `serverUntil`).
8. Tooling registration (see Context list): `run-package-script.mjs`, root `package.json` `purchases:*` aliases, `release-package.mjs` (`exampleVersion: "0.1.0"`), `check-package-consumer.mjs` (export checks `.`/`./server`, `requiredDocs` as media, fixtures: peer-free `--omit peer` install importing `@mrmeg/expo-purchases/server` and calling `parseRevenueCatWebhook`/`buildLedgerRows`; full fixture with peers + `react-native-purchases`/`-ui` type-checking an `index.tsx` that calls `createPurchases`, `createEntitlementStore`, renders `PurchasesProvider`/`PaywallGate`), `check-package-peer-compatibility.mjs` (`checkPackage("purchases")`, optional-peer assertion generalised to media and purchases), `package-compatibility-profiles.mjs` (`purchases`: Expo 57 and 58 profiles with `react`, `react-native`, `zustand`, `react-native-purchases 10.10.1`, `react-native-purchases-ui 10.10.1`), `check-package-consumer-profile.mjs` (`purchases` `App.tsx` branch; usage string lists purchases), `.github/workflows/package-compatibility.yml` (paths `packages/purchases/**`, matrix rows purchases/57 and purchases/58), `.github/workflows/publish-purchases.yml` (copy `publish-media.yml`, `workflow_dispatch` only with the `publish-lint.yml` header comment explaining why, package name/paths swapped), `jest.config.js` (`moduleNameMapper` for `@mrmeg/expo-purchases` and `/server`, `collectCoverageFrom`), root `tsconfig.json` `paths`, tests `runPackageScript.test.ts` (`PACKAGE_SLUGS`, `EXPECTED`, workflow list) and `packageToolingScripts.test.ts` (sorted lists).
9. Docs: `packages/purchases/README.md` (Ownership Boundary, Install with peer table, Public Imports, Client Setup with `createPurchases` + `PurchasesProvider` + `useEntitlement`, Gating with `PaywallGate`/`useRequireEntitlement`, Server Webhook with `createWebhookHandler` + Express/Convex/Expo-route snippets + the event → ledger table, **Identifier Contract** (entitlement / offering / product ids and `app_user_id` = the app's auth subject, case-sensitive), **Setup Checklist (human)** = Mindmap's seven steps generalised, **Store Review Checklist** (Terms of Use + Privacy Policy links visible on the paywall — RC dashboard paywall footer links and App Store Connect fields; standard Apple EULA; demo account; sandbox purchase/restore/cancel), Validation, Package Release, GitHub Publishing (dispatch-only, first publish needs `NPM_TOKEN`, then trusted publishing for `publish-purchases.yml`)), `LLM_USAGE.md`, `llms.txt`, `llms-full.md`, `CHANGELOG.md` (`## [0.1.0]` Added). Repo wiring: `scripts/build-llms-full.mjs` source, root `llms.txt`, `AGENTS.md` (Docs table + Tech Stack), `README.md` (architecture line, coverage sentence, publishing paragraph), `CONTRIBUTING.md:42`, `docs/template-modernization-guide.md` package list; `bun run docs:llms`.

## Validation

- Host rule: check `uptime` before each suite; if the 1-minute load is above 60, wait 60 s and re-check (up to 15 min).
- `bun install` (lockfile), then the autolinking check in step 1.
- `bun run packages:peer-check`, `bun run purchases:typecheck`, `bun run purchases:test`, `bun run purchases:build` (dist has `sdk.js` and `sdk.native.js`; `index.js` imports `./sdk` extension-less), `bun run purchases:pack` (tarball lists `dist`, `README.md`, `CHANGELOG.md`, `LLM_USAGE.md`, `llms.txt`, `llms-full.md`), `bun run purchases:consumer-smoke`.
- Root gates in the foreground with `timeout`: `bun run typecheck`, `bun run lint`, `bun run docs:llms:check`, `bun run docs:versions:check`, `bun run gen:templates:check`, `bun run gen:blocks:check`, `bun run ui:icons:check`, `bun run check:features`, `bun x jest --ci` sharded (`packages/purchases`, `scripts`, then the rest with `--maxWorkers=2`).
- `git diff --stat origin/dev` contains no `Agent/` file and no `npm publish` outside the dispatch-only workflow.

## Out of scope

- Migrating Mindmap, NeuroSpicy, or the camera app (Mindmap swaps `client/features/pro` after a publish; outline the swap in the PR body).
- Publishing 0.1.0 (Matt: dispatch `Publish Purchases Package` or `bun run purchases:release -- 0.1.0 --publish`; the first publish needs the `NPM_TOKEN` secret; then configure npm trusted publishing for `publish-purchases.yml` and add the `push` trigger).
- A rendered paywall screen or package UI beyond `PaywallGate`; RC dashboard paywall design; web billing (Stripe); server persistence (the consumer owns idempotency tables, ledger writes, and user lookup).
- Bundle-size baseline changes (the app does not import the package).

## Open questions

None.
