# Changelog

## [Unreleased]

- Updated the `Publish UI Package` workflow so pushes to `main` that change `packages/ui/package.json` publish the exact committed `@mrmeg/expo-ui` version when it is not already on npm. Manual workflow dispatch still supports bumping and committing a release version.

- Added `UIProvider` to `@mrmeg/expo-ui` so consumer apps can mount the package notification surface, status bar, and `@rn-primitives` portal host through the UI package instead of importing `@rn-primitives/portal` directly. `@rn-primitives/portal` is now a package dependency, the template root layout uses `UIProvider`, and the package docs/smoke fixture describe the new startup contract.

- Fixed the `Publish UI Package` workflow after trusted-publishing failures: publish now happens before committing future version bumps, exact-version reruns can retry an already-bumped release, npm is updated before publish, the UI package repository URL matches npm's trusted-publisher repository check, the publish step clears setup-node token config so npm can use OIDC, and a GitHub `NPM_TOKEN` secret can be used as a fallback publish credential.

- Added a manual `Publish UI Package` GitHub Actions workflow for `@mrmeg/expo-ui` trusted publishing. The workflow can bump the package version, run the UI gates and consumer smoke check, commit the version bump, and publish through npm OIDC without local npm login.

- Added `bun run ui:release` as a repo-root helper for `@mrmeg/expo-ui` releases. It bumps the package version, updates the lockfile, runs the package gates and packed consumer smoke check, and publishes with `npm publish --access public` only when `--publish` is passed.

- Fixed the packed `@mrmeg/expo-ui` consumer contract for fresh Expo SDK 55 apps: declared the i18n runtime peers used by `StyledText` and `Notification`, added Metro-friendly leaf exports for constants and hooks, and expanded `ui:consumer-smoke` to install the packed tarball into a clean Expo app and run an iOS export without a custom Metro config.

- Added an LLM-friendly consumer integration reference for the `@mrmeg/expo-ui` package, including supported imports, app startup wiring, web/native font behavior, peer dependency expectations, publish/update flow, and rules for keeping the package lightweight.

- Extracted the reusable UI surface into the workspace package `@mrmeg/expo-ui` under `packages/ui`. The package owns the 35 UI primitives, design tokens, theme/resource/motion hooks, theme and global notification stores, and UI helpers; package source now uses relative imports and no longer depends on `@/client/*`. The package intentionally does not ship font files: web loads Lato through Google Fonts, while native uses system sans-serif fallbacks. The template app, showcase registry, root layout, screens, features, form helpers, tests, and generator imports now consume UI through `@mrmeg/expo-ui` subpaths. Added package scripts for typecheck/test/build/publish dry-run plus a packed-package consumer smoke check, and documented npm publishing/consumer setup in README and Agent docs.

- Expanded reusable surface coverage and quieted the React `act(...)` warnings the existing billing tests were emitting. Added a shared `test/mockTheme.ts` helper that test files can `import "@/test/mockTheme";` at the top to get a stable light-scheme `useTheme` without mounting the full theme provider tree — keeps colour assertions deterministic and removes the per-file mock copy-paste. New behaviour-only tests (28 total): `Card.test.tsx` (composed Header/Title/Description/Content/Footer rendering, optional sections), `Badge.test.tsx` (every variant renders, default variant), `EmptyState.test.tsx` (title/description/icon/onAction wiring, action requires both `actionLabel` AND `onAction`), `Skeleton.test.tsx` (Skeleton/SkeletonText/SkeletonAvatar/SkeletonCard render), `RadioGroup.test.tsx` (controlled selection, `onValueChange` fires on label press, throws when an item is rendered outside the root), `client/lib/form/__tests__/form.test.tsx` (FormProvider + FormTextInput + FormCheckbox integration via a small `useForm` + `zodResolver` harness — submits typed value, validation blocks submit and surfaces the Zod message), `client/screens/__tests__/screens.test.tsx` (WelcomeScreen renders title/subtitle/primary action/footer + onPress fires; ErrorScreen variant defaults render and explicit overrides win + primary action wires; ListScreen renders items via `renderItem`, shows the empty-state title/description when data is empty, hides the empty UI when `loading`). Wrapped the `useAuthStore.setState` calls in `client/features/billing/__tests__/useBillingActions.test.tsx` `beforeEach`/`afterEach` in `act(...)` so React Test Renderer no longer logs "updates not wrapped in act" warnings between cases — the warnings made real test failures harder to spot in `bun run test:ci` output. CONTRIBUTING.md gains a testing-focus paragraph naming the shared mock helper and the reusable-surface coverage policy. 424 tests across 53 suites, +28 from this change.
- Created `client/showcase/registry.ts` as the single source of truth for the screen templates, demos, and design-system components surfaced through the Explore tab and the component count badge. The Explore screen (`app/(main)/(tabs)/index.tsx`) previously hardcoded a `screenTemplates` array (13 entries), a `demos` array (4 entries), and a literal `"35 components"` badge — three places that all had to be updated by hand whenever a screen template, demo, or component was added or removed. The registry now exports typed `SCREEN_TEMPLATES`, `DEMOS`, and `COMPONENTS` arrays with stable `id` fields, plus a `getComponentCount()` helper, and Explore reads all three. Per the spec the showcase metadata extraction is the first pass — the 2,169-line `app/(main)/(demos)/showcase/index.tsx` file is left alone for now and is wired through the registry only via the count badge. New `client/showcase/__tests__/registry.test.ts` (10 tests) pins the contract: ids unique within each list, routes unique within each list, every screen-template + demo route maps to a real `.tsx` file under `app/`, every component import path maps to a real `.tsx` file under `client/components/ui/`, import paths stay under the `@mrmeg/expo-ui/components/` namespace, and `getComponentCount()` matches the array length. CONTRIBUTING.md "Adding a New Component" gains a registry-update step; the duplicate "Adding a New Screen Template" sections were merged into a single "Adding a New Screen Template Or Demo" with the registry step. 396 tests across 46 suites, +10 from this change.
- Restored the GitHub Actions CI workflow that an earlier `chore: remove GitHub CI` commit deleted, so every push and PR to `main` / `dev` runs the same `bun run` gates an adopter would run locally — without that, regressions in scaffolding, route handlers, and the new feature-isolation contract could land unnoticed and propagate into every project cloned from the template. New `.github/workflows/ci.yml` runs two parallel jobs: `validate` (Bun setup → `bun install --frozen-lockfile` → `bun run typecheck` → `bun run lint` → `bun run check:features` → `bun run test:ci`) and `bundle-size` (build → `bun run bundle-size` against the checked-in baseline). Both jobs are credential-free — tests mock the AWS / Stripe surfaces and the build runs against a blank env. Concurrency cancels stale runs on rebases. `Agent/AGENTS.md` re-adds the CI row in the Tech Stack table; `Agent/Docs/ARCHITECTURE.md` adds a CI bullet alongside the local-verification line; `Agent/Docs/PERFORMANCE.md` notes the bundle-size delta is now PR-gated; `README.md` gains a CI section explaining both jobs and how to reproduce CI locally; `CONTRIBUTING.md` PR checklist adds the feature-isolation check and a "CI is green" line.
- Centralized app identity in a single configurable module so renaming the template no longer requires hunting through `app.config.ts`, `app.json`, and `client/features/billing/hooks/useBillingActions.ts` for the same five values. New `app.identity.ts` is the source of truth: it defines the template defaults (`name: "template"`, `slug: "template"`, `scheme: "myapp"`, `iosBundleIdentifier: "com.mrmeg.template"`, `androidPackage: "com.mrmeg.template"`), reads `EXPO_PUBLIC_APP_*` overrides via direct property access (so Metro inlines them into the client bundle and Node sees them at config-load time), trims whitespace-only values back to defaults, and validates each override at parse time — bad schemes (`"Has Spaces"`), non-reverse-DNS bundle ids, and non-kebab slugs throw with field-named messages so a typo can't quietly ship into TestFlight. New `client/lib/identity.ts` exposes the matching runtime accessors (`getAppScheme`, `getAppName`, `buildAppDeepLink`) backed by the same defaults. `app.config.ts` now reads `getAppIdentity()` for `name`, `slug`, `scheme`, `ios.bundleIdentifier`, `android.package`, and surfaces `extra.appScheme` for any code that prefers `Constants.expoConfig` over env. `useBillingActions.ts`'s `nativeReturnUrl()` now uses `buildAppDeepLink("/billing/return")` instead of the hardcoded `"myapp://billing/return"`. Removed the redundant top-level `app.json` (Expo prefers the dynamic `app.config.ts` whenever both exist; keeping the static duplicate created two more places to forget when renaming). New tests: `__tests__/appIdentity.test.ts` (11 cases — defaults, whitespace fallback, valid overrides, scheme/bundle/slug validation rejection, name allows free-form, `buildDeepLink` path normalization) and `client/lib/__tests__/identity.test.ts` (6 cases — runtime defaults, env override flow, whitespace fallback, deep-link composition). `.env.example` lists all five new vars with comments on safe defaults; README adds a "Renaming the Template" section with the env-var checklist and the `expo prebuild` reminder for native; `Agent/Docs/BILLING.md` and `Agent/Docs/APP_OVERVIEW.md` reframe `myapp://billing/return` as the default-and-overridable scheme rather than a hardcoded literal. 386 tests across 45 suites, +17 from this change.
- Defined and enforced the feature dependency contract. The previous "features never import from other features (except media → notifications)" rule was both too strict (`features/app` legitimately composes `features/auth` + `features/onboarding`, and `features/billing` reads `features/auth`'s identity store) and stale (the media → notifications exception didn't actually exist — media uses `client/state/globalUIStore`, which lives in the shared layer). New `scripts/check-feature-isolation.js` walks every `client/features/<f>` folder, finds `@/client/features/<g>/...` imports, and validates against an `ALLOWED_DEPENDENCIES` allowlist that names exactly two cross-feature edges: `app → {auth, onboarding}` (shell composition) and `billing → auth` (identity-only). All other features (`auth`, `onboarding`, `media`, `i18n`, `notifications`, `navigation`, `keyboard`) are explicitly self-contained. The script exits 0 today (8 features, 0 violations) and is wired up two ways: `bun run check:features` for local one-off runs, and `client/features/__tests__/featureIsolation.test.ts` (3 tests) so the same scan runs as part of `bun run test:ci`. The Jest contract checks (1) zero violations, (2) every feature folder is documented in the allowlist (no silent additions), (3) the documented shell-composition edges still exist (so the allowlist can't drift into pure aspiration). `Agent/Docs/ARCHITECTURE.md` rewritten: replaces the false-absolute "never cross-import" rule with the actual matrix, lists the shared layer (`client/lib/*`, `client/hooks/*`, `client/components/ui/*`, `client/constants/*`, `client/state/*`, `shared/*`, `@rn-primitives`) as always-allowed, includes a "copy-with" note per feature so adopters know exactly which folders/files travel together when extracting a feature into another project, and points at the script + test as the enforcement seam. Added `bun run check:features` to `package.json` scripts. 369 tests across 43 suites, +3 from this change.
- Aligned `Agent/Docs/BILLING.md` and `Agent/Docs/API.md` with the implemented `-session` billing routes. The implementation has always posted to `/api/billing/checkout-session` and `/api/billing/portal-session` with `{ planId, interval, returnPath }` (price id resolution lives server-side in `app/api/billing/_shared/env.ts`), but BILLING.md's flow diagrams, server-surface table, and edge-case row still referenced the historical `/api/billing/checkout` / `/api/billing/portal` names with a raw `priceId` body, and one rate-limit paragraph in API.md still framed the strict-limit registration as future work. Updated both docs to: name `/api/billing/checkout-session` + `/api/billing/portal-session` consistently in flows, the server-surface table, the rate-limit paragraphs, and the auth edge-case row; describe the body as `{ planId, interval, returnPath }` with a callout that clients must never send raw price ids; document `400 unknown-plan` and `422 configuration-missing` outcomes alongside the existing routes; and note that the webhook is intentionally unrate-limited because Stripe retries can burst faster and signature verification already gates abuse. The current `STRICT_LIMIT_PATHS` registration in `server/rateLimits.js` already lists both `-session` paths and `client/features/billing/api.ts` already targets them, so no implementation change was needed. 366 tests still green.
- Refreshed the public-facing template docs so a new project owner cloning the repo doesn't hit stale commands or paths. **README.md** rewritten end-to-end: every script switched to `bun` (`bun install`, `bun run web`, `bun run typecheck`, `bun run lint`, `bun run test:ci`, `bun run start-local`, `bun run analyze`); the Architecture diagram replaced with the actual layout (`client/features/{auth,billing,media,i18n,notifications,onboarding,keyboard,navigation,app}/`, `client/lib/{api,form,storage,devtools}/`, `client/state/`, `app/api/`, `server/`, `shared/`, `Agent/`); the Theming snippet uses real tokens (`theme.colors.background`, `foreground`, `card`, `border` — the old `bgPrimary`/`textPrimary` did not exist); the API Layer section documents both `apiClient` (typed discriminated-union responses, `setAuthToken`) and `authenticatedFetch` (Amplify-aware, used by media + billing hooks); the Tech Stack section lists the actual icon library (`@expo/vector-icons` Feather set) and font (`@expo-google-fonts/lato`, no Merriweather) and pins versions to what's in `package.json`; a new "Optional features" subsection explicitly calls out auth, billing, media, and Sentry as off-by-default with the env-var name to flip each on, mirroring the typed `503 *-disabled` contract the routes ship; added a "Generator CLI" callout matching the modernized scaffolds; added `bundle-size` and `analyze` to the script table. **CONTRIBUTING.md**: PR checklist switched to `bun run lint` / `bun run typecheck` / `bun run test:ci` / `bun run web`; testing notes describe the actual coverage scope (`client/**`, `app/api/**`, `server/**`, `shared/**`); Project Structure section now points at `Agent/Docs/ARCHITECTURE.md` instead of the gitignored `CLAUDE.md`, lists the real directories (`client/features/*`, `client/lib/api`, `client/lib/form`, `client/lib/storage`, `client/state`, `server/`, `shared/`); Design System note tightens the `StyleSheet.flatten([...])` rule with the React Native Web crash reason instead of pointing at `CLAUDE.md`; "Adding a New Component" now uses `bun run generate component` and points the showcase demo at `app/(main)/(demos)/showcase/`. No code changes — `bun run test:ci` (366/366) / `typecheck` / `lint` all still green.
- Hardened the media feature for unconfigured-template installs: a fresh clone with no R2/S3 env vars no longer fails through opaque 500s on the Media tab. New `server/api/media/storage.ts` reads the four `R2_*` env vars (whitespace-only treated as missing, same normalization as `validateClientEnv`) and returns a discriminated `{ kind: "configured" } | { kind: "unconfigured", missing }` value. All four media routes (`list`, `getUploadUrl`, `getSignedUrls`, `delete` for both DELETE single and POST batch) call it before any work — when storage is unconfigured they return a typed `503 media-disabled` body (`{ code, message, missing: string[] }`) and never construct an `S3Client` or make a presigner call. The body lists only env-var *names*, never values, even when secrets happen to be set; OPTIONS preflight still succeeds so browsers can read the response. The `S3Client` is now built lazily and cached per endpoint, with a test-only `_resetMediaStorageForTests()` escape hatch. New `client/features/media/lib/problem.ts` mirrors the existing `BillingProblem` pattern: `MediaError` carries a `MediaProblem` discriminated union (`disabled` / `bad-request` / `unauthorized` / `unknown`), `toMediaError(response)` maps non-OK responses including the typed `503` body (defensively filtering non-string entries from `missing[]`), and `shouldRetryMediaError` keeps React Query from burning retries on disabled / bad-request / unauthorized while preserving the default soft retry budget for unknown errors. All four media hooks (`useMediaList`, `useMediaUpload`, `useSignedUrls`, `useMediaDelete` / `useMediaDeleteBatch`) now throw `MediaError` and apply `retry: shouldRetryMediaError` where applicable. The Media tab (`app/(main)/(tabs)/media.tsx`) renders three new branches: a setup-state with cloud-off icon + missing-vars list (testID `media-disabled`) when disabled, a retryable error state with the destructive icon + Retry button (testID `media-error`) for transient fetch failures, and the existing empty/list rendering for the configured paths. Upload buttons are disabled when storage is unconfigured. New tests: `app/api/media/__tests__/mediaDisabled.test.ts` (11 tests covering `503 media-disabled` behavior across all four routes — disabled with all four missing, disabled with only one missing, whitespace-only treated as missing, OPTIONS preflight still succeeds, S3 mock never invoked, response body never echoes credential values) and `client/features/media/lib/__tests__/problem.test.ts` (13 tests covering all four `MediaProblem` mappings, the missing-list filter for non-string entries, the JSON-parse fallback for non-JSON bodies, `isMediaError` narrowing, and the retry budget semantics). Updated the existing `delete.test.ts` to set all four R2 env vars and reset the storage cache between tests so the configured-path coverage stays green. API.md, USER_FLOWS.md, and `.env.example` updated to document the disabled response and the setup state. 366 tests across 42 suites, +22 from this change.
- Modernized `bun run generate` so scaffolds match the template's current architecture without manual cleanup. **Screens** now write both `client/screens/<Name>Screen.tsx` (the reusable, named-export template) and `app/(main)/(demos)/screen-<kebab>.tsx` (a demo route that just renders the template), matching the pattern every existing screen template (`SettingsScreen`, `ListScreen`, `WelcomeScreen`, …) follows. **Forms** now write to `client/components/forms/<Name>Form.tsx` but use the shared `@/client/lib/form` primitives (`FormProvider`, `FormTextInput`, `useForm`, `zodResolver`) instead of pasting react-hook-form boilerplate, ship a typed schema with an inferred `<Name>FormData` type, and drop the misleading "Run: bun add react-hook-form …" footer (those deps already ship with the template). **Hooks** now emit a working `useState`-backed scaffold with typed `<Name>Options` / `<Name>Result` instead of a TODO body that immediately fails lint, and no longer import unused `useEffect`/`useCallback`. **Components** drop the unused `getShadowStyle` destructure. The CLI surface is unchanged, name validation/normalization (Pascal/kebab/snake) is preserved, and writes stay atomic — collisions are detected up front so a partial-write can't leave the tree half-scaffolded. The pure helpers (`getPlannedFiles`, `validateName`, `toPascalCase`, `toKebabCase`, …) are now exported so unit tests can pin the contract without touching the filesystem. New `scripts/__tests__/generate.test.ts` adds 22 tests covering name normalization (Pascal, camel, kebab boundaries), validation rejection/warning rules, and per-type generated content (no unused imports, correct paths, primitives wired correctly, FormProvider passed via the named `form` prop rather than spread). README, CONTRIBUTING, and APP_OVERVIEW updated to reflect the new behavior; CLAUDE.md is gitignored and untouched. 344 tests across 40 suites, +22 from this change.
- Aligned client env validation with the template's optional-feature contract: a fresh clone with the default `.env.example` no longer prints `Missing required client environment variables` for Cognito + the external API URL, since both Cognito (`EXPO_PUBLIC_USER_POOL_ID` / `EXPO_PUBLIC_USER_POOL_CLIENT_ID`) and `EXPO_PUBLIC_API_URL` are intentionally opt-in (the template uses `app/api/*` Expo Router routes by default and the prod config falls back to a placeholder when the URL is empty). `validateClientEnv` now warns only when Cognito is half-configured (exactly one of the two pool vars set) or when `EXPO_PUBLIC_BILLING_ENABLED` is truthy with `EXPO_PUBLIC_APP_URL` blank — and it normalizes whitespace-only values as missing across the board so `EXPO_PUBLIC_USER_POOL_ID="   "` is treated as unset. Env reads moved inside the function via direct `process.env.EXPO_PUBLIC_*` property access (the form Expo's static analyzer inlines) so tests can vary inputs across calls. New `client/lib/__tests__/validateEnv.test.ts` pins the contract with 12 cases (default-empty, both-set, each-half-of-Cognito, whitespace-as-missing for both groups, billing flag accepted as `"true"` / `"1"` / `"TRUE"`, billing-with-url silent, billing-disabled silent, missing API URL never warns even with full Cognito). Updated `.env.example` comments to label Cognito and the API URL as optional, refreshed `Agent/Docs/APP_OVERVIEW.md` "How to Use" to lead with "run as-is", added the `EXPO_PUBLIC_API_URL` row to `Agent/Docs/API.md`'s env table with the optional-with-fallback note, and corrected the `app/_layout.tsx` callsite comment that incorrectly claimed validation throws in production. 322 tests across 39 suites, +12 from this change.
- Ignored Jest coverage output: `bun run test:ci` writes `clover.xml`, `coverage-final.json`, `lcov-report/`, and `lcov.info` into a top-level `coverage/` directory, but `.gitignore` only covered `dist/`, `.expo/`, native folders, and env files — so every CI-style test run dirtied the working tree with untracked files. Added `coverage/` to `.gitignore` (in a new `# test coverage output` block next to the existing tooling sections). The `test:ci` script and Jest reporters are unchanged; coverage still generates locally for inspection.
- Restored `bun run lint`: `expo lint` was failing with `Cannot find package 'globals'` and could not even load the ESLint flat config, so every clone of the template inherited a broken quality gate. The fix adds the three packages the config actually imports (`globals`, `@eslint/js`, `@eslint/eslintrc`) to `devDependencies`, plus pins `@typescript-eslint/parser` directly at `^8.58.2` so it tracks the existing plugin version (the transitive `8.46.3` parser bundled an older `@typescript-eslint/scope-manager` that lacks ESLint 10's `addGlobals` API). Pinned `settings.react.version` to `"19.2"` instead of `"detect"` because `eslint-plugin-react@7.37.5` still calls the removed `context.getFilename()` API during version detection and crashes rule loading on ESLint 10. The flat-config shape is otherwise unchanged: no rules touched, no plugin upgrades, no Expo lint replacement. `bun run lint` now exits 0; the 148 pre-existing source warnings are out of scope for this spec and left for a future style sweep.
- Fixed Expo Router route leakage for API helpers: server-only auth and billing support modules moved out of `app/` into `server/api/shared/*` and `server/api/billing/*`, so Expo no longer generates accidental `/api/_shared/*` and `/api/billing/_shared/*` static routes or tries to bundle the Node-only Stripe SDK into native client exports. `build-sourcemap` now matches the repo's intended workflow by exporting the web bundle with source maps into `dist/`.
- Expanded Jest coverage to the real risk areas: auth bootstrap, onboarding gating, and the media server contracts. `jest.config.js` now collects coverage from `app/api/**`, `server/**`, and `shared/**` in addition to `client/**`, with `__tests__/` and index barrels excluded so CI can flag drift in CORS, rate-limiting, auth bootstrap, and the FFmpeg worker path — the regressions the night-shift specs kept uncovering, not just happy-path UI. New suites: `client/features/auth/__tests__/config.test.ts` (5 tests pinning the env-validation contract: warn-but-don't-throw in development, throw in production, list only the missing var, idempotency in the warn branch, module-reset behavior — scoped to the synchronous pre-Amplify path because Jest's default babel transform can't rewrite `await import("aws-amplify")`, noted inline with the tradeoff), `client/features/auth/__tests__/authStore.test.ts` (7 tests on the sync setter/reset surface that AuthWrapper, ProfileTab, and the gates depend on — `initialize`/`signOut`/Hub hit the same dynamic-import path as config, so those stay exercised in the running app), `client/features/onboarding/__tests__/onboardingStore.test.ts` (9 tests covering the native AsyncStorage path, the web `window.localStorage` path, null-read fallback, and swallowed storage errors — Platform.OS is mutated on the live react-native module instead of remocking it so TurboModule shims don't get pulled in), and `app/api/media/__tests__/delete.test.ts` (7 tests covering the OPTIONS preflight, missing-key 400, single-delete happy path with the `DeleteObjectCommand` input assertion, 500 on S3 failure with CORS headers intact, POST 400s for missing/oversized key arrays, and POST success with `Deleted`/`Errors` echo — `@aws-sdk/client-s3` is fully mocked via `mock` prefixed factory vars so nothing reaches R2). Fixed an incidental regression in the onboarding test file where `jest.spyOn` on top of the AsyncStorage mock's existing `jest.fn()` produced a pollution trail across tests; switched to a behavior-level assertion (call `setHasSeenOnboarding`, then read storage back) instead. 28 new tests, 310 total across 38 suites.
- Fixed web FFmpeg worker serving: both `metro.config.js` (dev) and `server/index.ts` (prod) previously read the worker from `client/lib/videoConversion/ffmpeg-worker.js`, a path that no longer existed after the media feature-folder refactor, so `fs.existsSync` quietly failed and the `/_expo/static/js/web/ffmpeg-worker.js` route was never registered in either runtime. The client then fetched whatever the SSR fallback returned (HTML) and hung inside `FFmpeg.load()`. A new CommonJS helper `server/ffmpegWorker.js` is the single source of truth for both the URL (`FFMPEG_WORKER_URL = /_expo/static/js/web/ffmpeg-worker.js`) and the repo-relative path (`FFMPEG_WORKER_RELATIVE_PATH = client/features/media/lib/videoConversion/ffmpeg-worker.js`); both runtimes now `require()` it. `convert.ts` gains a typed `FFmpegWorkerUnavailableError` and `toBlobURL` throws that error on non-OK status or `text/html` content-type instead of wrapping the HTML into a worker blob. `useMediaLibrary` switches the toast to `Video Converter Unavailable` when that specific error is thrown, keeping the existing graceful fallback to uploading the original format. New regression tests (`server/__tests__/ffmpegWorker.test.js`, `client/features/media/lib/videoConversion/__tests__/convert.test.ts`) assert the file-path exists, the URL contract matches `config.ts`, and the error class keeps its name/url/status contract so a future refactor can't silently break web conversion again (10 new tests, 282 total across 34 suites). PERFORMANCE.md now documents the worker-path contract and graceful-failure model; ARCHITECTURE.md's media-pipeline diagram references the optional web conversion step.
- Wired the pricing and account surfaces to live billing state: `PricingScreen` now takes optional billing-aware fields (`planId`, `isCurrent`, `actionLabel`, `actionState`, `disabledReason`, `loading`) so a single reusable component can render static demos or fully state-driven subscription pages. New `derivePricingPlan` / `derivePlanActionState` helpers in `client/features/billing/lib/pricing.ts` fold a `BillingSummary` + catalog into those fields (current plan → `current` + disabled CTA; free plan for an entitled user → `downgrade-disabled` with a "Cancel through Manage subscription" hint; unauthenticated viewers always see "Sign in to continue"). New `useBillingActions` hook handles the `POST /api/billing/checkout-session` and `POST /api/billing/portal-session` calls plus the browser handoff — web uses `window.location.href`, native uses `expo-web-browser`'s `openAuthSessionAsync` with `myapp://billing/return` — and invalidates the billing summary query on return. The pricing demo (`app/(main)/(demos)/screen-pricing.tsx`) now drives those hooks with a local `PlanDefinition[]` catalog and folds the typed `BillingProblem` union into user-facing notifications (`unknown-plan`, `configuration-missing`, `no-customer`, `billing-disabled`, etc.). The profile tab gained a single **Manage Subscription** / **Upgrade** CTA (chooses between them based on `customerId`/entitlement), plus explicit warning rows for `cancelAtPeriodEnd` and `past_due`. New shared return route at `app/billing/return.tsx` displays a neutral processing/cancel/portal message while the summary refetches. DESIGN.md and USER_FLOWS.md now document the CTA state machine and warning-row rules; BILLING.md's client-surface table reflects the real hook names. Tightened the server bootstrap signatures (`readBillingEnv`, `ensureAuthBootstrapped`, `ensureBillingBootstrapped`) to accept `Record<string, string | undefined>` so tests don't need to fake `NODE_ENV`. Added 20 new tests (pricing view-model derivation, `useBillingActions` happy path + typed problem surfacing + dismissed flow, return-URL parser) — 272 total across 32 suites.
- Added the Stripe subscriptions bootstrap and config: new `app/api/billing/_shared/env.ts` reads the `STRIPE_*` surface (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID_<PLAN>_MONTH|YEAR`) and folds the per-plan price ids into a catalog, flagging any unset price ids without failing. New `app/api/billing/_shared/stripeAdapter.ts` lazy-constructs the `stripe@22.0.1` client behind the already-defined `StripeCustomersPort` + session/webhook factories — it handles both legacy and modern `current_period_end` placement, escapes metadata search queries, and keeps raw Stripe types inside the adapter. New `app/api/_shared/cognitoTokenVerifier.ts` wires `aws-jwt-verify@5.1.1` into the pluggable `TokenVerifier` port. Two matching lazy bootstraps — `app/api/_shared/authBootstrap.ts` and `app/api/billing/_shared/bootstrap.ts` — read env on first request, honor any preinstalled verifier/registry (so unit tests can still inject fakes), fall through to `null` when env is missing, and are idempotent across repeated calls. Every `/api/billing/*` route now calls `ensureAuthBootstrapped()` / `ensureBillingBootstrapped()` on entry, so unconfigured deployments still return the existing typed `503 billing-disabled` + fail-closed `401` responses without ever importing the Stripe SDK. `checkout-session+api.ts` now resolves the server-owned price id per request and passes it on the request object, keeping plan-catalog knowledge out of the adapter. Client config gains `billingEnabled` + `appUrl` (from `EXPO_PUBLIC_BILLING_ENABLED` / `EXPO_PUBLIC_APP_URL`), and `validateEnv` warns on half-configured billing and on billing-enabled-without-app-url. BILLING.md now ships with a fresh-Stripe-account setup walkthrough (CLI install, products/prices, `.env`, `stripe listen`, exercise) and a "Disabling billing cleanly" section; API.md's env table lists every billing variable; README.md gains a short Billing section. Added 14 new tests (env parsing, both bootstraps with preinstalled-registry preservation + idempotency) — 252 total across 30 suites.
- Added the authenticated billing API foundation: four Expo Server routes under `app/api/billing/` — `GET /summary`, `POST /checkout-session`, `POST /portal-session`, `POST /webhook` — all driven by a process-wide `BillingRegistry` so unconfigured deployments return a typed `503 billing-disabled` instead of throwing. Protected routes share `requireAuthenticatedUser` from new `app/api/_shared/auth.ts`, which extracts the bearer token, delegates to a pluggable `TokenVerifier` port, and fails closed with a structured 401 when no verifier is registered. Checkout maps `{ planId, interval }` onto a server-owned Stripe price id (400 `unknown-plan` with the catalog ids; 422 `configuration-missing` when the interval has no price) and surfaces resolver linking conflicts as 409 `billing-conflict`. The webhook route reads `request.text()` before any JSON parsing so the bytes Stripe signed match the bytes verified, dispatches through a `WebhookHandler` port, and short-circuits duplicate deliveries via a swappable `IdempotencyStore` (default in-memory LRU; multi-instance deployments inject a shared store via `setWebhookIdempotencyStore`). Checkout + Portal routes are now registered in `STRICT_LIMIT_PATHS` (10/min); the webhook is not, since Stripe retries can burst faster and its signature requirement already gates abuse. New `client/features/billing/api.ts` + `lib/problem.ts` fold the typed server errors into a `BillingProblem` discriminated union so UI code handles `billing-disabled`, `billing-conflict`, `no-customer`, `configuration-missing`, and `unknown-plan` without reading HTTP status codes. API reference now documents every route, body shape, and error code. Added 31 new tests (auth helper, idempotency store, all four routes with injected fakes, client problem mapping) — 238 total.
- Added the billing identity contract and normalized subscription state: new `shared/billing.ts` defines the `BillingSummary` shape, `freeBillingSummary`, `isEntitled`, `normalizeStripeSubscription`, and a default plan catalog. New `app/api/billing/_shared/` introduces `BillingAccountResolver` + `createStripeBillingAccountResolver` with deterministic customer-linking rules (metadata lookup → single-match email backfill → `CustomerConflictError` → create) and reads billing state without auto-creating customers. New `client/features/billing/` exposes the `useBillingSummary` React Query hook as the single entry point for subscription state — the profile tab now reads plan label, renewal date, and status from it instead of hard-coded "Free" / "Active" labels. Raw Stripe SDK types do not cross the client boundary. Added unit coverage for shared billing helpers and the resolver (17 new tests, 204 total).
- Defined the default Stripe subscriptions architecture: new `Agent/Docs/BILLING.md` names Stripe Checkout + Billing Portal in `hosted-external` mode as the template baseline, with webhook-authoritative server state, a normalized `BillingSummary` keyed to Cognito `sub`, a single `/billing/return` contract (web `https://…`, native `myapp://billing/return`), and explicit applicability guardrails (native PaymentSheet, App Store / Play Store in-app purchase, metered billing, tax, and team seats are out of scope for the baseline). ARCHITECTURE, API, APP_OVERVIEW, DOMAIN, and USER_FLOWS docs all reference the billing baseline.
- Wired the app shell to auth and onboarding: new `client/features/app/` module (`useAppStartup`, `AuthGate`, `OnboardingGate`, `isAuthEnabled`) owns startup sequencing and per-surface auth policy. Splash stays visible until fonts, i18n, onboarding, and (optionally) Amplify bootstrap have resolved. Onboarding is rendered inline by the root layout on first run — the main Stack never mounts during onboarding. Profile and Settings tabs wrap their exports in `<AuthGate>`, so signing out replaces those tabs with `AuthScreen` inline while unprotected surfaces stay browsable. The template remains fully explorable without Cognito env vars. Added unit coverage for `isAuthEnabled` and `AuthGate`.
- Fixed pre-existing Jest failures (react-test-renderer pin, expo-font / vector-icons / safe-area-context mocks, Progress accessibility role, Switch thumb tree assertion, InputOTP onComplete via simulated changeText).
- Fixed upload rate-limit alignment: the strict 10-requests-per-minute limiter now covers `/api/media/getUploadUrl` (the real presigned-upload route). Configuration moved to `server/rateLimits.js` with regression coverage so the path cannot drift back to the stale `/api/media/upload-url`.
- Fixed web media delete: shared CORS helper now advertises `DELETE` so browser preflights to `DELETE /api/media/delete?key=...` succeed from allowed origins. Added regression coverage in `app/api/_shared/__tests__/cors.test.ts`.
- Fixed false-positive client env warnings on web by validating `EXPO_PUBLIC_*` values with Expo-compatible direct property access.
- Disabled RN core `useNativeDriver` on web to remove repeated dev warnings from showcase and demo animations.
- Blurred focused web navigation elements before route changes to avoid `aria-hidden` warnings when hidden screens retained focus.
- Fixed the web accordion wrapper so uncontrolled accordions no longer flip into controlled mode on first toggle.
