import { Platform } from "react-native";

// This component is no longer needed - navigation bar theming is handled
// by expo-navigation-bar plugin in app.json
export function NavigationBarBackground() {
  return null;
}
